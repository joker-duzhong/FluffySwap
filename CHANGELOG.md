# Changelog

## 2026-05-08

- 将我的作品列表和生成中轮询状态提升到 `historyStore`，我的页与我的作品页共享同一份作品状态。
- 我的作品页检测到进行中任务后改为轮询用户历史列表，不再调用单个任务状态接口刷新。
- 首页模板分类固定增加“全部”入口，默认展示未分类作品可见的完整列表。
- 按设计图优化会员开通页视觉高度、套餐卡层级、按钮间距和选中态样式。
- 按设计图重做邀请页为“说明页 + 海报分享弹层”结构，补齐微信好友、朋友圈和保存相册入口。
- 新增全局半屏创作组件，首页自由创作、模板详情同款和我的作品页入口统一使用，提交后跳转我的作品页轮询生成状态。
- 调整小程序登录流程为启动静默登录，未绑定手机号视为未登录，并通过手机号授权按钮完成绑定。
- 新增我的页资料编辑半屏，支持昵称更新和头像上传后保存。
- 补充模板、作品、积分、会员等页面的局部骨架屏、按钮 loading、空状态和接口错误 toast。
- 首页新增微信小程序发送给朋友和分享到朋友圈能力。
- 删除小程序页面中的设计稿模拟状态栏和手绘胶囊，改为系统安全区与胶囊透明占位。
- 新增统一顶部导航组件，基于微信胶囊位置收敛各页面顶部安全距离和右侧占位。
- 修复首页空态滚动、底部 Tab 图标激活状态、毛玻璃样式、自由创作按钮和“我的作品”快捷入口。
- 优化模板详情、生成结果、会员、积分、邀请和作品页的返回入口及按钮文字垂直居中。
- 将登录统一为带遮罩的半弹窗，并新增用户协议、隐私政策页面及真实跳转。
- 提升登录半弹窗层级，避免被首页底部 Tabbar 遮挡。
- 按设计稿细化“我的”页头部、会员卡、作品区和菜单列表布局。
- 重构 AuraKey 微信小程序前端为“模板 / 我的”双 Tab 结构，移除旧发现、作品、创作抽屉等冗余框架代码。
- 首页模板分类改为读取 `/api/v1/aurakey/gallery/categories`，模板列表与详情使用 gallery 相关接口。
- 按设计稿重做模板首页、模板详情、我的页、登录页、会员页、积分明细页、邀请海报页、作品管理页和生成页。
- 新增 `aurakeyApi` 语义化接口层，统一保留后端返回结构并做页面数据适配。
- 新增前端二维码渲染能力，邀请海报使用 `uqrcodejs` 生成二维码矩阵。
- 将 Figma 导出的静态素材重命名为语义化文件名，并集中到 `src/config/assets.ts` 引用。
- 删除旧 `AuraIcon`、旧 SVG 图标配置和旧 API 二次封装，保留更干净的 common/page components 结构。
- 删除未使用的 `src/lib` OpenAPI 生成 SDK，并移除 `@hey-api/openapi-ts` 依赖。
